<Window x:Class="PcInfoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PcInfoApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.DataContext>
        <local:PcInfo/>
    </Window.DataContext>

    <Grid Background="#191C20">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Name="Pcspecsbtn" Content="PC specs" Click="Pcspecsbtn_Click"></Button>
            <Button Name="Minimize"  Grid.Column="1" Content="Minimize" Click="Minimize_Click"></Button>
            <Button Name="Close" Grid.Column="2"  Content="Close" Click="Close_Click"/>
        </Grid>
        <Grid Name="ViewGrid" Grid.Row="1">
        </Grid>
    </Grid>
</Window>
