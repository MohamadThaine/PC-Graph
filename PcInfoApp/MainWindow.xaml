<Window x:Class="PcInfoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PcInfoApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1000"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.DataContext>
        <local:PcInfo/>
    </Window.DataContext>

    <Grid Background="#191C20">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition Width="AUTO"></ColumnDefinition>
                <ColumnDefinition Width="AUTO"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Style="{StaticResource NoGlow}" Name="Pcspecsbtn" Click="Pcspecsbtn_Click" Height="Auto" Width="70" Background="{x:Null}" BorderBrush="{x:Null}">
                <StackPanel>
                    <Image Source="/imgs/PcIcon.png" Width="50" ></Image>
                    <TextBlock Text="Pc monitor" Foreground="WHITE" FontFamily="/fonts/#Prototype"></TextBlock>
                </StackPanel>
            </Button>
            <Button Style="{StaticResource NoGlow}" Name="Minimize"  Grid.Column="1" Click="Minimize_Click" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Image Source="/imgs/MinimizeIcon.png" Width="30"></Image>
            </Button>
            <Button Style="{StaticResource NoGlow}" Name="Close" Grid.Column="2"  Click="Close_Click" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Right" VerticalAlignment="Top" Width="50">
                <Image Source="/imgs/CloseIcon.png" Width="30"></Image>
            </Button>
            <Grid Height="1" Background="White" Grid.ColumnSpan="3" VerticalAlignment="Bottom"></Grid>
        </Grid>
        <Grid Name="ViewGrid" Grid.Row="1">
        </Grid>
    </Grid>
</Window>
